name: security_app
description: Aplicación de seguridad básica
version: 1.0.0+1
environment:
  sdk: '>=3.8.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.1
  dart_jsonwebtoken: ^3.4.1
  flutter_secure_storage: ^11.0.0
  flutter_dotenv: ^6.0.1
  get_it: ^9.2.1
  go_router: ^18.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_secure_storage_platform_interface: ^2.0.3
flutter:
  assets:
    - .env
